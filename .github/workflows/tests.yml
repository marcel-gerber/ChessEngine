name: Tests

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        uses: cmake --build build --target ChessEngineTests

      - name: Run tests
        uses: ctest